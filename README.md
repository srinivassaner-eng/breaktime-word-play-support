# BreakTime: Word Play — Support

Public support and privacy page for the iOS app **BreakTime: Word Play**.

Served by GitHub Pages from `index.html` on the `main` branch.

The page uses no cookies, no analytics and no tracking, matching the app's
privacy promise. It is a single static HTML file with inline CSS and no
JavaScript.
